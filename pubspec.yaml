## I am so mad at headache of a process this made me go through to get things working. It better work. I swear.
name: birdn_flutter_tutorial_app
description: "A new Flutter project."

publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 0.0.1+1

environment:
  sdk: ^3.6.0


dependencies:
  flutter:
    sdk: flutter

  english_words: ^4.0.0
  provider: ^6.1.2
  ## https://pub.dev/packages/word_generator/install
  word_generator: ^0.5.0
  ## https://pub.dev/packages/grammer
  grammer: ^1.0.3

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
